

"You are GitHub Copilot assisting with implementing a small change in the `PodDigitizer` repo.

Goal: (APPROVED_PLAN_TITLE)

Scope: (FILES_OR_DIRS_TO_CHANGE)

Requirements:
- Small, incremental commits with clear messages
- Include / update tests where applicable
- Ensure no secrets are added
- Follow `brain/memory/conventions.md` and `brain/memory/constraints.md`

Deliverables (for each step):
1. Modify only the listed files. Exact files to create or edit, with short code snippets for the changes.
2. Commands to run locally (e.g., tests, linters, build steps).
3. Keep changes minimal and explicit.

Implementation Plan:


